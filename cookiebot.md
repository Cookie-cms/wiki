> [!NOTE]  
> Work in progress
# CookieBot

CookieBot — это Discord-бот на Python для CookieCMS. Он позволяет:

- Искать Discord пользователя по нику из игры и наоборот.
- Для админов: повышать или понижать ранг пользователя.
<!-- 
## Установка

1. Установите Python 3.10+  
2. Склонируйте репозиторий бота:
   ```
   git clone https://github.com/your-repo/cookiebot.git
   cd cookiebot
   ```
3. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```
4. Создайте `.env` файл и добавьте токен вашего Discord-бота:
   ```
   DISCORD_TOKEN=ваш_токен
   ```
5. Запустите бота:
   ```
   python bot.py
   ```

## Основные команды

- `/find_nick <discord>` — найти ник в игре по Discord.
- `/find_discord <nick>` — найти Discord по нику в игре.
- `/rank_up <user>` — повысить ранг (только для админов).
- `/rank_down <user>` — понизить ранг (только для админов).

Подробнее — в исходном коде и help-командах бота. -->
